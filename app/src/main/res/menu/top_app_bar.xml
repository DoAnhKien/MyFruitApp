<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search"
        android:title="Tìm kiếm"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <item
        android:id="@+id/mn_add_category"
        android:title="@string/add_category_info"
        app:showAsAction="never" />

    <item
        android:id="@+id/mn_edit_category"
        android:title="@string/edit_category_info"
        app:showAsAction="never" />

    <item
        android:id="@+id/mn_delete_category"
        android:title="@string/delete_category_info"
        app:showAsAction="never" />

</menu>